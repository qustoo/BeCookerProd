<%= form_with(model: recipe_attachment) do |form| %>
  <% if recipe_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_attachment.errors.count, "error") %> prohibited this recipe_attachment from being saved:</h2>

      <ul>
        <% recipe_attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recipe_id %>
    <%= form.number_field :recipe_id %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
